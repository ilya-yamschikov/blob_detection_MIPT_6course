\documentclass[11pt]{article}
\usepackage{amsmath, amsfonts, amssymb}
\usepackage[english, russian]{babel}
\usepackage{graphicx}
\usepackage{subfigure}
\usepackage{color}
\usepackage{indentfirst}

\usepackage{geometry}
\geometry{left=2cm}
\geometry{right=1.5cm}
\geometry{top=1cm}
\geometry{bottom=2cm}

\DeclareMathOperator*{\argmax}{arg\,max}

\begin{document}

\section*{ Поиск светлых пятен}

Рассматриваем задачу обработки изображений. Требуется на монохромном изображении выделить светлые пятна - однородные яркие участки с резким изменением градиента яркости на границе.

В данной задаче под изображением понимаем матрицу $Z = \{z_{ij}\}, i=1,\dots,m, j=1,\dots,n$ где $z_{ij} \in B$ - яркость пиксела.
Здесь $B \in \mathbb{R}_+$ - возможные значения яркости. В компьютерной графике обычно $B = \{1,\dots,256\}$. Светлые пятна описываются эллипсами.

\textbf{Интерфейс:} таким образом на вход алгоритм получает изображение $Z$, а на выход выдает множество эллипсов $\{\varepsilon_1,\dots,\varepsilon_k\}$ - светлых пятен изображения.

Для нахождения светлых пятен воспользуемся алгоритмом LoG-детектор (Laplacian-of-Gaussian). Производится свертка изображения с лаплассианой гауссианы для нахождения светлых пятен. Затем перебором параметра гауссианы ищется характерный размер пятна.

\begin{thebibliography}{9}
\bibitem{MS04} Krystian Mikolajczyk and Cordelia Schmid, Scale and affine invariant interest point detectors,
\emph{International Journal of Computer Vision}, pp. 63--86, 2004
\end{thebibliography}
\end{document} 